# 🚀 BertyPro Ultra - منصة خدمات النزهة 2

**BertyPro Ultra** هو تطبيق ويب تقدمي (PWA) متكامل يهدف لخدمة سكان منطقة "النزهة 2" و"جسر السويس". يعمل التطبيق كحلقة وصل ذكية وسريعة بين السكان ومقدمي الخدمات (فنيين، توصيل، طوارئ) باستخدام تقنيات الموقع الجغرافي والذكاء الاصطناعي البسيط.

---

## ✨ المميزات الرئيسية

يوفر التطبيق تجربة مستخدم سلسة تركز على السرعة والسهولة:

* **📱 تطبيق ويب تقدمي (PWA):** قابل للتثبيت على الهاتف ويعمل بكفاءة عالية (Manifest + Service Worker).
* **📍 خرائط تفاعلية:** تكامل مع **Mapbox** لتحديد موقع العميل بدقة وعرض الخريطة داخل التطبيق.
* **🎙️ البحث الصوتي:** إمكانية البحث عن الخدمات باستخدام الأوامر الصوتية.
* **🆘 نظام الطوارئ السريع:** أزرار عائمة للاتصال الفوري بالإسعاف (123) أو النجدة (122).
* **💬 الطلب عبر واتساب:** تحويل الطلبات وتفاصيل الموقع والمستخدم تلقائياً إلى رسالة واتساب جاهزة للإرسال لمسؤول الخدمة.
* **🤖 مساعد ذكي (Chatbot):** واجهة محادثة آلية للرد على استفسارات العملاء.
* **💾 حفظ البيانات محلياً:** استخدام `LocalStorage` لحفظ بيانات العميل (الاسم، الهاتف، العنوان) لسهولة الطلب في المرات القادمة.
* **🎨 تصميم عصري:** واجهة داكنة (Dark Mode) مع ألوان نيون (`#00ff88`) وتأثيرات بصرية جذابة.

---

## 🛠️ التقنيات المستخدمة

تم بناء المشروع باستخدام تقنيات الويب الحديثة (Vanilla JavaScript) لضمان الخفة والسرعة:

* **HTML5 & CSS3:** تصميم متجاوب (Responsive) باستخدام CSS Variables و Flexbox/Grid.
* **JavaScript (ES6+):** المنطق الأساسي للتطبيق بدون إطارات عمل ثقيلة.
* **Mapbox GL JS:** لعرض الخرائط وتحديد المواقع.
* **Turf.js:** لحساب المسافات والعمليات الجغرافية المتقدمة.
* **Web APIs:**
    * `Geolocation API`: لتحديد الموقع.
    * `Vibration API`: لتأثيرات اللمس (Haptic Feedback).
    * `SpeechRecognition API`: للبحث الصوتي.

---

## 📂 هيكلة المشروع

```text
BertyPro-Ultra/
├── index.html          # الصفحة الرئيسية (واجهة التطبيق)
├── about.html          # صفحة من نحن
├── privacy.html        # سياسة الخصوصية
├── login.html          # صفحة دخول الإدارة (Mockup)
├── manifest.json       # إعدادات PWA
├── css/
│   └── styles.css      # ملف التنسيقات الأساسي
├── js/
│   ├── main.js         # نقطة الدخول والتحكم الرئيسي
│   ├── request.js      # منطق طلب الخدمات والواتساب
│   ├── user.js         # إدارة بيانات المستخدم
│   ├── map.js          # إعدادات الخرائط (Mapbox)
│   ├── voice.js        # منطق البحث الصوتي
│   ├── chatbot.js      # منطق المحادثة الآلية
│   └── utils.js        # أدوات مساعدة (حساب مسافات، وقت)
└── data/
    └── app-config.js   # (مطلوب) ملف إعدادات الخدمات وأرقام الهاتف
    